<div class="container" style="width: 200px; padding: 30px;">
  <%= form_with(model: @usage, local: true) do |form| %>
    <div class="form-group">
      <%= form.label :units_used %><br>
      <%= form.number_field :units_used, class: 'form-control form-input' %>
    </div>
    <div class="sub form-group">
      <%= form.hidden_field :subscription_id, value: @usage.subscription_id %>
    </div>
    <div class="feature form-group">
      <%= form.hidden_field :feature_id, value: @usage.feature_id %>
    </div>
    <div>
      <%= form.submit "Create Usage", class: 'btn btn-success'%>
    </div>
  <% end %>
</div>
