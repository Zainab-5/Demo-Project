<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item <%= active?('/subscriptions') %>" >
          <a class="nav-link" href="/subscriptions">Subscriptions </a>
        </li>
        <li class="nav-item <%= active?('/plans') %>">
          <a class="nav-link" href="/plans">Plans</a>
        </li>
      </ul>
    </div>
    <% if user_signed_in? %>
      <% if current_user.image.attached? %>
        <a class="navbar-brand" href= '/users/edit' >
          <%= image_tag current_user.image, class: "style_image" %>
        </a>
      <% else %>
        <%= link_to "Edit Profile ", edit_user_registration_path, class: 'left-link' %>
      <% end %>
      <%= link_to "Logout", destroy_user_session_path, class: 'left-link', :method => :delete %>
    <% end %>
</nav>

